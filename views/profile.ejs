<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Mini Social - Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
    
    body {
      font-family: 'Poppins', sans-serif;
    }
    
    .gradient-bg {
      background: linear-gradient(to right, #111827, #1f2937);
    }
    
    .btn-primary {
      transition: all 0.3s ease;
      background: linear-gradient(to right, #3B82F6, #2563EB);
      box-shadow: 0 4px 6px -1px rgba(59, 130, 246, 0.4);
    }
    
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 10px -1px rgba(59, 130, 246, 0.5);
    }
    
    .btn-danger {
      transition: all 0.3s ease;
      background: linear-gradient(to right, #EF4444, #DC2626);
      box-shadow: 0 4px 6px -1px rgba(239, 68, 68, 0.4);
    }
    
    .btn-danger:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 10px -1px rgba(239, 68, 68, 0.5);
    }
    
    .btn-warning {
      transition: all 0.3s ease;
      background: linear-gradient(to right, #F59E0B, #D97706);
      box-shadow: 0 4px 6px -1px rgba(245, 158, 11, 0.4);
    }
    
    .btn-warning:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 10px -1px rgba(245, 158, 11, 0.5);
    }
    
    .card {
      backdrop-filter: blur(16px) saturate(180%);
      background-color: rgba(31, 41, 55, 0.75);
      border-radius: 12px;
      border: 1px solid rgba(255, 255, 255, 0.125);
      box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
      transition: all 0.3s ease;
    }
    
    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 40px 0 rgba(0, 0, 0, 0.5);
    }
    
    .textarea-field {
      transition: all 0.3s ease;
      border: 2px solid #374151;
      resize: none;
    }
    
    .textarea-field:focus {
      border-color: #3B82F6;
      box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.3);
      transform: translateY(-2px);
    }
    
    .post-appear {
      opacity: 0;
      transform: translateY(20px);
      animation: fadeInUp 0.6s ease-out forwards;
    }
    
    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body class="gradient-bg min-h-screen text-white">
  <div class="max-w-4xl mx-auto p-6 animate__animated animate__fadeIn">
    <div class="flex justify-between items-center mb-8 p-4 card animate__animated animate__slideInDown">
      <h1 class="text-2xl md:text-3xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-blue-400 to-blue-600">
        My Profile
      </h1>
      <div class="flex space-x-3">
        <a href="/home" class="btn-primary px-4 py-2 rounded-lg text-sm font-medium animate__animated animate__fadeIn">Feed</a>
        <a href="/logout" class="btn-danger px-4 py-2 rounded-lg text-sm font-medium animate__animated animate__fadeIn animate__delay-1s">Logout</a>
      </div>
    </div>
    
    <div class="mb-10 animate__animated animate__fadeInUp">
      <div class="card p-6 mb-8">
        <div class="flex items-center mb-4">
          <div class="h-16 w-16 rounded-full bg-gradient-to-br from-blue-400 to-blue-600 flex items-center justify-center text-2xl font-bold">
            <%= user.name.charAt(0).toUpperCase() %>
          </div>
          <div class="ml-4">
            <h2 class="text-2xl font-bold text-white">Hello, <%= user.name %></h2>
            <p class="text-blue-300">@<%= user.username %></p>
          </div>
        </div>
        
        <form action="/post" method="post" class="mt-6 animate__animated animate__fadeIn animate__delay-1s">
          <div class="mb-3">
            <label class="block text-blue-300 mb-2 text-sm font-medium">Create a new post</label>
            <textarea
              name="content"
              placeholder="What's on your mind?"
              class="w-full bg-gray-800/50 textarea-field rounded-lg p-3 text-white placeholder-gray-400 outline-none min-h-[100px]"
              required
            ></textarea>
          </div>
          <button
            type="submit"
            class="btn-primary px-6 py-2 rounded-lg text-white font-medium inline-flex items-center"
          >
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-11a1 1 0 10-2 0v2H7a1 1 0 100 2h2v2a1 1 0 102 0v-2h2a1 1 0 100-2h-2V7z" clip-rule="evenodd" />
            </svg>
            Create Post
          </button>
        </form>
      </div>
    </div>
    
    <div class="posts space-y-6">
      <h3 class="text-xl font-semibold text-blue-300 mb-4 animate__animated animate__fadeIn">Your Posts</h3>
      
      <% user.posts.reverse().forEach(function(post, index) { %>
        <div class="card p-5 post-appear" style="animation-delay: <%= 0.1 * index %>s">
          <div class="mb-3">
            <h3 class="text-lg font-semibold text-blue-400">@<%= user.username %></h3>
          </div>
          
          <p class="text-gray-200 text-lg mb-4 leading-relaxed"><%= post.content %></p>
          
          <div class="flex items-center justify-between mt-2 pt-3 border-t border-gray-700">
            <span class="bg-gray-800 px-3 py-1 rounded-full text-sm">
              <%= post.like.length %> likes
            </span>
            <div class="flex gap-3">
              <a href="/like/<%= post._id %>" class="flex items-center px-3 py-1 rounded-lg text-sm <%= post.like.indexOf(user._id) == -1 ? 'text-blue-400 hover:text-blue-300' : 'text-pink-500 hover:text-pink-400' %>">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-1" fill="<%= post.like.indexOf(user._id) == -1 ? 'none' : 'currentColor' %>" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
                </svg>
                <%= post.like.indexOf(user._id) == -1 ? "Like" : "Unlike" %>
              </a>
              <a href="/edit/<%= post._id %>" class="btn-warning px-3 py-1 rounded-lg text-sm font-medium flex items-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                </svg>
                Edit
              </a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
  
  <script>
    // Add staggered animation for posts
    document.addEventListener('DOMContentLoaded', function() {
      const posts = document.querySelectorAll('.post-appear');
      
      posts.forEach((post, index) => {
        setTimeout(() => {
          post.classList.add('animate__animated', 'animate__fadeInUp');
        }, index * 150);
      });
    });
  </script>
</body>
</html>
