<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tailwind EJS Test</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
      <div class="flex justify-end">
        <a href="/logout" class="bg-red-500 px-4 py-2 rounded-md text-sm"
          >Logout</a
        >
      </div>
      <h5 class="mb-5">Update your post</h5>
      <form action="/update/<%=post._id%>" method="post">
        <textarea
          name="content"
          placeholder="what's on your mind"
          class="resize-none w-1/3 bg-transparent outline-none border-2 border-zinc-700 rounded-md p-2 block"
        ><%=post.content%></textarea>
        <input
          type="submit"
          name=""
          value="Update post"
          class="bg-yellow-600 px-3 py-1 mt-2 rounded-md text-sm"
        />
      </form>
     
    </div>
  </body>
</html>
